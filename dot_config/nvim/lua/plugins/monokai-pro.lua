return {
  {
    "loctvl842/monokai-pro.nvim",
    lazy = true,
    -- colorscheme "monokai-pro" (default "pro" filter, dark). A "monokai-pro-light"
    -- filter also exists if a light slug is ever added. See ~/.config/theme/registry.zsh.
    opts = {},
  },
}
