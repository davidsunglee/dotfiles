return {
  {
    "oxfist/night-owl.nvim",
    lazy = true,
    -- colorscheme "night-owl" (dark only). night-owl-light maps to onelight in
    -- lua/config/theme.lua since there's no first-class Night Owl Light for nvim.
    -- See ~/.config/theme/registry.zsh.
    opts = {},
  },
}
