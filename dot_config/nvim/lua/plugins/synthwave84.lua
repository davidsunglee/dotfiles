return {
  {
    "LunarVim/synthwave84.nvim",
    lazy = true,
    -- colorscheme "synthwave84" (dark only; Lua, standalone — the colors/ wrapper
    -- calls .load() with defaults). Best-of-field but stale upstream (Feb 2023).
    -- Optional glow config via require("synthwave84").setup{ glow = {...} }.
    -- See ~/.config/theme/registry.zsh.
  },
}
