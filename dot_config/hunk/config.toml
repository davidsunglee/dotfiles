# Managed by ~/.config/theme (unified theme controller). The theme line below is
# regenerated from the THEME_HUNK map in ~/.config/theme/registry.zsh (mapped
# from the active slug) — edit the map, not this line. Keep the fences intact.
# hunk v0.17+ has exact built-ins for nearly every slug, so this matches the
# real palette instead of the old `theme = "auto"` (which OSC-queried the
# terminal bg and fell back to github-dark-default when unanswered).
# Press `t` inside hunk to preview other themes at runtime.
# >>> theme-controller (managed) >>>
theme = "nord"
# <<< theme-controller (managed) <<<
